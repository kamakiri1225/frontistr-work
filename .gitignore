# ==========================================================
#  FrontISTR / CAE 計算結果・生成物（重いので追跡しない）
#  再実行すれば作り直せるファイルはここに列挙する。
# ==========================================================

# --- ソルバー実行時の出力・ログ類 ---
FistrModel.res.*
FistrModel.restart.*
FistrModel.vis_psf.*/
vtkMeshData/
*.vtu
*.pvtu
*.vtk
FSTR.dbg.*
*.msg
*.sta
0.log
run.log
run_dumph.log
meshHeaderData.bin
*.cnt.bak

# --- 重い行列・配列のダンプ（スクリプトで再生成できる） ---
*.npy
*.npz
*.mtx
*.csr
*.rhs
*.mm
dump_matrix_*
K_*.csv

# --- Python ---
__pycache__/
*.pyc

# --- OS ---
.DS_Store
Thumbs.db
